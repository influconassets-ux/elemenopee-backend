services:
  - type: web
    name: elemenopee-backend
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URL
        sync: false
    healthCheckPath: /health
    autoDeploy: true
